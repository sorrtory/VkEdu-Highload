# 3. Глобальная балансировка нагрузки

Самая тяжелая часть этого приложения это лента.
Поэтому для пользователей мы создаем inbox,
куда будем класть посты заранее и не мучить базу при каждом запросе (fanout on write).
Алгоритм формирования inbox будет основополагающим и должен улучшаться бесконечно.

Если вкратце, мы будем в зависимости от активности пользователя брать его
подписки и рекомендации (популярные посты ~~подписки людей с похожими интересами~~).
Для активных пользователей можно собирать сотни постов, а для менее активных - десятки или меньше.
Триггером будет служить публикация нового поста сообществом
и запрос новых постов в ленте пользователем (здесь будет нужна хорошая очередь, чтобы не положить систему).
Для популярных сообществ (например, 100к+ подписчиков) посты можно сразу класть в CDN как популярные,
либо раскидывать постепенно+выборочно.

Так, клиент при открытии ленты запрашивает с бека посты.
Мы отдаем ему ссылки на популярные посты с CDN,
остальные посты отдаются как запрос на inbox (+запросы в базу).
Итак, лента = CDN (популярные посты) + inbox (остальные подписки).

На 1000 км идет задержка 10 мс.
Мы работаем только по РФ и 70% пользователей находятся в ее европейской части.
Важно обеспечить быструю загрузку контента для европейской части России,
остальные регионы будут терпеть более высокий RTT.
Наше приложение не предполагает рилтайм, поэтому даже 10_000 км ~= 200 мс
RTT для получения поста в ленте не будет непреодолимой преградой.

## Домены

- `vk.ru` - главная страница
- `api.vk.ru` - API сообщений, лайков, комментариев и т.д.
- `media.vk.ru` - S3-хранилище для медиафайлов (в основном юзерфото)
- `login.vk.ru` - авторизация
- `mycdn.me` - CDN для статики, медиафайлов и популярных постов

<!-- - `ovu.mycdn.me` - загруженные медиафайлы -->
<!-- - `vkuserphoto.ru` - раздача фотографий -->
<!-- - `vkvdX.okcdn.ru` - раздача видео -->
<!-- - `queuev4.vk.ru` - раздача постов в ленте (рекомендации) -->
<!-- - `pu.vk.ru` - загруженные файлы -->

## Размещение датацентров

Датацентры пытаемся размещать в местах с хорошей городской инфраструктурой,
чтобы обеспечить высокую скорость физического доступа, надежность,
низкую цену за электричество и обслуживание.

Три равных датацентра ставим в Петербурге, Москве и Екатеринбурге.
Сервисы делаем независимыми от состояния, запускаемся в кубере.
Базу (тексты + юзерфото) реплицируем между всеми тремя датацентрами
для общих данных (пользователи, посты, комментарии) и
для важных данных (сообщения, настройки, подписки).

Для ленты будем использовать шардирование по региону.
Так inbox пользователей с Владивостока будет находится в ближайшем ЦОДе в ЕКБ и тп.

На чтение ходим только в ближайшую реплику, а при записи общие данные разносим между всеми тремя датацентрами.

В датацентрах закладываем избыточность.
Имея 33%+33%+34% инфраструктуры на [300к RPS](./02.md#rps),
мы хотим позволить себе потерять один датацентр и продолжать работу без деградации.
То есть каждый датацентр в идеале держит пик на 100к RPS с запасом.

В целях экономии имеем не чистый N+1 на 150к, а, пожалуй, где-то 125к RPS на датацентр.
В случае падения одного датацентра, будем иметь механизм ослабления работы inbox.
Будем раздавать ленту только из CDN, inbox будем обновлять с задержкой.

Уточню, что большая часть RPS идет из текстового месседжера,
что кушает довольно [мало трафика](./02.md#сетевой-трафик) в сравнении с лентой и не требует высокой скорости ответа.

Поэтому даже при падении одного датацентра, мы будем продолжать обслуживать пользователей,
пусть и с некоторой деградацией ленты.

### Балансировка API

<!-- Невозмутимо отправляем пользователей из Москвы и Питера в Екатеринбург при первой возможности (например в полночь по Москве, когда ЕКБ уже спит, а Москва наиболее активна) -->

Принимаем тот факт, что пользователи с Дальнего Востока пойдут за API в ЕКБ на ~7000 км = 70мс => RTT = 140мс.
Для комментариев и сообщений это не заметно, а вот медиафайлы и ленту будем отдавать через CDN.

Для API используем Anycast балансировку: ходим в один ЦОД (настраиваем весами + внутри приложения),
и там у нас лежит inbox пользователя.

В случае перестройки маршрутов пользователь будет автоматически перенаправлен на другой ЦОД
и потеряет доступ к своему inbox. В целом, для нас это не критично.
Мы сможем грузить посты с CDN, а inbox будем обновляться с задержкой.

Однако в случае падения одного датацентра.
Мы будем вынуждены обработать множество генераций постов на лету.
Для этого используем очередь в каждом датацентре, и напишем
фронт так чтобы посты грузились по мере их готовности, а не по порядку.

Есть правда еще неудобный момент - это фолз негатив перестройка маршрутов,
когда пользователи будут прыгать между датацентрами.
Поэтому мы внутри нашего приложения прибиваем каждого пользователя к одному датацентру по хешу и
проксирум его запросы туда (пингуем с таймаутом, иначе кладем в очередь на генерацию новых постов),
тогда при перестройке маршрутов будем подгружать его данные из родного ЦОДа.
Это не идеально, но BGP не должен так часто прыгать между отдаленными датацентрами.

<!-- При запросе `vk.ru` отсылаем две A записи в зависимости от региона. -->
<!-- Так, пользователь из Казани получит IP адреса 1. ЕКБ, 2. Москвы и тп. -->

<!-- Так, пользователь из Казани получит один IP адрес в зависимости от числа хопов/нашег 1. ЕКБ, 2. Москвы 3.
Таким образом при падении датацентра в ЕКБ, мы лишимся inbox этого пользователя,
но постепенно подгрузим его через Москву. -->

<!-- ~~Можно также пытаться играться с весами: около 12:00 UTC+3 в МСК пик активности,  а ЕКБ уже засыпает, либо скейлиться в кубере в часы пик и прочее прочее~~ -->

## CDN

Ставим CDN в крупных городах для обеспечения быстрой работы по всей России:
Москва, Волгоград, Екатеринбург, Новосибирск, Хабаровск.

На CDN кладем статику, популярные медиафайлы (аватарки) и популярные посты.
Например, делаем пометку в базе, что пост популярный и при запросе на `api.vk.ru`
отдаем ссылку на `mycdn.me`, а не `media.vk.ru`.

CDN вне зависимости от региона содержит одни и те же данные и в общем используется для
повышения пропускной способности и раздачи нашей ленты.
Статика и аватарки весят копейки, так что смело кладем в CDN
(аватарки в худшем случае 80_000_000 \* 2КБ / 1024 / 1024 = 152.5 ГБ, что не так дорого)

<!-- Можно усложнять с мудрыми таблицми расположения пользователей и
http балансировкой на беке (например для дальнего востока) но чета нихачу пока -->

Алгоритм определения популярности поста может быть прост:

- популярный паблик - все его посты популярные
- крутой пост, который алгоритм считает хорошей рекомендацией
- пост, который набрал много лайков и комментариев за короткое время

### Балансировка CDN

CDN определяем по Latency-based DNS: ходим туда, где быстрее всего.

<!-- Обязательно устанавливаем CDN в отдаленных от центра России регионах (в Сибири и на Дальнем Востоке), чтобы обеспечить быструю загрузку контента для всех пользователей, независимо от их местоположения. -->
<!-- Учитываем что большинство пользователей находятся в европейской части России -->

<!-- Используем балансировку как минимум на раздаче фотографий (`sunX-Y.vkuserphoto.ru`) и видео (`vkvdX.okcdn.ru`), как на самых ресурсоемких компонентах. -->

<!-- Для выбора ближайшего CDN используем Anycast балансировку, который позволяет направлять запросы на ближайший доступный сервер. -->
<!-- То есть анонсируем один айпишник и bgp будет направлять пользователей на наиболее доступный сервер. -->

<!-- Также имеет смысл воспользоваться взвешанной DNS балансировкой, чтобы распределять трафик между датацентрами в соответствии с долей инфраструктуры и текущей нагрузкой. Например, если Москва перегружена, часть трафика может быть перенаправлена в Санкт-Петербург. -->

<!-- Также можно использовать географическую балансировку. Например, когда пользователь с дальнего востока уснули, а в восточной Сибири -->
<!-- наоборот стали наиболее активны, то трафик можно пускать не в Екатеринбург, а в Хабаровск. -->

## Источники

- [Инфраструктура ВК](https://www.tadviser.ru/index.php/%D0%A1%D1%82%D0%B0%D1%82%D1%8C%D1%8F:VK_%28%D0%98%D0%A2-%D0%B8%D0%BD%D1%84%D1%80%D0%B0%D1%81%D1%82%D1%80%D1%83%D0%BA%D1%82%D1%83%D1%80%D0%B0%29)
- [Карта магистралей в РФ](https://www.comnews.ru/sites/default/files2019/vision-files/mapmagistr2020-2.pdf)
- [Карта CDN вк на RIPE](https://www.ripe.net/participate/forms/uploads/fobi_plugins/file/capif-1-presentation-upload/Elena%20Yakupova%20The%20peering%20policy%20of%20the%20VK%20Company_cd02e936-adf0-4f27-a41e-dbf4aea2a670.pdf)

# 6. Физическая схема БД

### Денормализация

Связи на ER-диаграмме показывают происхождение snapshot-данных и канонические сущности, а не необходимость читать несколько таблиц для одного ответа.

- `USERS`: разделены на `USERS` для авторизации и `USER_PROFILES` для публичного профиля пользователя.

- `USER_SESSIONS`: хранит canonical refresh-сессии в PostgreSQL. Redis используется только как TTL-кеш для быстрых проверок access/session key и может быть полностью восстановлен из PostgreSQL.

- `USER_PROFILES`: хранит `username`, `display_name`, `bio`, `avatar_url`, `avatar_preview_url`, город и страну. Эти данные копируются snapshot-полями в посты, комментарии, участников сообществ и сообщения.

- `COMMUNITIES`: добавлены snapshot-поля аватарки и счетчиков:
    - `avatar_preview_url`;
    - `members_count_snapshot`;
    - `posts_count_snapshot`;
    - `counters_updated_at`.

- `COMMUNITY_COUNTERS`: добавлена отдельная таблица для актуальных счетчиков сообщества:
    - `members_count`;
    - `posts_count`.

- `COMMUNITY_MEMBERS`: добавлены snapshot-поля пользователя и сообщества, чтобы читать список участников из одной таблицы:
    - `user_username_snapshot`;
    - `user_display_name_snapshot`;
    - `user_avatar_url_snapshot`;
    - `community_slug_snapshot`;
    - `community_name_snapshot`;
    - `community_avatar_url_snapshot`.

    Для крупных сообществ добавлен `member_bucket = hash(user_id) % N`, чтобы список участников не жил в одной огромной партиции.

- `COMMUNITY_MEMBERS_BY_USER`: обратная read-model для списка сообществ пользователя.

- `POSTS`: добавлены snapshot-поля автора, сообщества, вложений и счетчиков:
    - `author_username_snapshot`;
    - `author_display_name_snapshot`;
    - `author_avatar_url_snapshot`;
    - `community_slug_snapshot`;
    - `community_name_snapshot`;
    - `community_avatar_url_snapshot`;
    - `attachment_refs`;
    - `comments_count_snapshot`;
    - `reactions_count_snapshot`;
    - `counters_updated_at`.

    Для ленты сообщества добавлен `time_bucket`, чтобы посты крупного сообщества не накапливались в одной бесконечной партиции.

- `POSTS_BY_AUTHOR`: отдельная read-model для чтения постов автора без перестройки запроса по основной ленте сообщества.
  В ключ также добавлен `time_bucket`, чтобы ограничить размер партиции активного автора.

- `POST_COUNTERS`: добавлена отдельная таблица для актуальных счетчиков поста:
    - `comments_count`;
    - `reactions_count`.

- `COMMENTS`: добавлены snapshot-поля автора комментария:
    - `author_username_snapshot`;
    - `author_display_name_snapshot`;
    - `author_avatar_url_snapshot`.

- `POST_REACTIONS`: разделена на две таблицы:
    - `POST_REACTIONS_BY_POST` - для уникальности реакции на пост и списка лайкнувших;
    - `USER_REACTIONS_BY_USER` - для быстрого определения `is_liked_by_me` в ленте.

    В `POST_REACTIONS_BY_POST` добавлен `reaction_bucket = hash(user_id) % N`, чтобы реакции вирусного поста не попадали в одну горячую партицию.

- `FILE_OBJECTS`: вместо хранения файла в БД хранит metadata файла:
    - `storage_bucket`;
    - `storage_key`;
    - `storage_key_hash`;
    - `cdn_url`;
    - `preview_url`;
    - `mime_type`;
    - `size_bytes`;
    - `checksum`.

    Горячий путь чтения metadata идет по короткому `file_id`, а не по длинному `storage_key`.

- `POST_ATTACHMENTS`: добавлены snapshot-поля файла, чтобы читать вложения без обращения к `FILE_OBJECTS`:
    - `cdn_url_snapshot`;
    - `preview_url_snapshot`;
    - `mime_type_snapshot`;
    - `size_bytes_snapshot`.

- `USER_FEED_ITEMS`: стала готовой карточкой поста для персональной ленты. Добавлены snapshot-поля:
    - тела поста;
    - автора;
    - сообщества;
    - вложений;
    - счетчиков комментариев и реакций.

- `POPULAR_FEED_ITEMS`: добавлена отдельная read-model для популярной ленты. Хранит готовые карточки популярных постов.

- `CONVERSATION_PARTICIPANTS`: добавлены snapshot-поля участника:
    - `user_username_snapshot`;
    - `user_display_name_snapshot`;
    - `user_avatar_url_snapshot`.

- `CONVERSATIONS`: добавлен `home_dc`, чтобы все операции конкретного диалога
  маршрутизировались в один домашний ДЦ и писались в соответствующий messenger
  keyspace (`messenger_msk`, `messenger_spb`, `messenger_ekb`) с
  `LOCAL_QUORUM`.

- `CONVERSATION_PARTICIPANTS_BY_USER`: обратная read-model для проверки диалогов пользователя и получения списка доступных диалогов.

- `USER_CONVERSATIONS`: добавлена read-model inbox пользователя:
    - название/аватар диалога для отображения списка;
    - последнее сообщение;
    - отправитель последнего сообщения;
    - `unread_count`.

- `MESSAGES`: добавлены snapshot-поля отправителя:
    - `sender_username_snapshot`;
    - `sender_display_name_snapshot`;
    - `sender_avatar_url_snapshot`.

<!--
### Проверка денормализации

Горячие пользовательские сценарии читаются из одной таблицы или read-model. Канонические таблицы нужны для записи, уникальности и восстановления производных представлений.

| Сценарий чтения              | Таблица / read-model        | Почему не требуется чтение связанных таблиц                                |
| :--------------------------- | :-------------------------- | :------------------------------------------------------------------------- |
| Профиль пользователя         | `user_profiles`             | Публичные поля профиля лежат вместе                                        |
| Страница сообщества          | `communities`               | Метаданные, превью аватарки и snapshot счетчиков лежат в строке сообщества |
| Участники сообщества         | `community_members`         | Есть snapshot имени, username и аватарки пользователя                      |
| Сообщества пользователя      | `community_members_by_user` | Есть snapshot slug, имени и аватарки сообщества                            |
| Лента сообщества             | `posts`                     | Пост содержит snapshot автора, сообщества, вложений и счетчиков            |
| Посты автора                 | `posts_by_author`           | Отдельная проекция содержит готовую карточку поста автора                  |
| Комментарии поста            | `comments`                  | Комментарий содержит snapshot автора                                       |
| Лайкнул ли пользователь пост | `user_reactions_by_user`    | Проверка идет по ключу `(user_id, post_id)`                                |
| Персональная лента           | `user_feed_items`           | Карточка поста уже содержит автора, сообщество, вложения и счетчики        |
| Популярная лента             | `popular_feed_items`        | Карточка популярного поста хранится готовой                                |
| Inbox мессенджера            | `user_conversations`        | Есть snapshot названия/аватара диалога, последнего сообщения и отправителя |
| История сообщений            | `messages`                  | Сообщение содержит snapshot отправителя                                    |
| Поиск                        | `search_index_entries`      | Поисковый документ содержит текст, preview и avatar preview                |

### Проверка MVP

Физическая схема покрывает MVP из [ДЗ 1](./01.md) и не добавляет отдельные продуктовые области вне него.

| MVP из ДЗ 1                                 | Таблицы физической схемы                                                                                            |
| :------------------------------------------ | :------------------------------------------------------------------------------------------------------------------ |
| Регистрация и авторизация                   | `users`, `user_sessions`                                                                                            |
| Создание и редактирование профиля           | `user_profiles`                                                                                                     |
| Создание и администрирование сообществ      | `communities`, `community_counters`                                                                                 |
| Публикация постов, фотографий и видео       | `posts`, `posts_by_author`, `file_objects`, `file_payloads`, `post_attachments`                                     |
| Комментарии и лайки к постам                | `comments`, `post_reactions_by_post`, `user_reactions_by_user`, `post_counters`                                     |
| Лента популярных и рекомендованных постов   | `user_feed_items`, `popular_feed_items`                                                                             |
| Подписка на сообщества                      | `community_members`, `community_members_by_user`                                                                    |
| Поиск по пользователям и контенту           | `search_index_entries`                                                                                              |
| Мессенджер для общения между пользователями | `conversations`, `conversation_participants`, `conversation_participants_by_user`, `user_conversations`, `messages` |

`event_outbox` - служебная таблица надежной доставки изменений в read-model и поисковый индекс. Она не расширяет MVP как продуктовая функция.
-->

### Счетчики

Мы выносим счетчики в отдельные таблицы как canonical source,
а в основных таблицах храним snapshot-значения.

Пример: лайк поста

- Пишем лайк в `POST_REACTIONS_BY_POST`.
- Пишем лайк в `USER_REACTIONS_BY_USER`.
- Инкрементим `POST_COUNTERS.reactions_count`.
- Асинхронно обновляем snapshot:
    - `POSTS.reactions_count_snapshot`
    - `USER_FEED_ITEMS.reactions_count_snapshot`
    - `POPULAR_FEED_ITEMS.reactions_count_snapshot`

```mermaid
erDiagram

    USERS {
        bigint id PK
        varchar email UK
        varchar phone UK
        varchar password_hash
        smallint status
        varchar home_region
        timestamptz created_at
        timestamptz updated_at
        timestamptz last_login_at
    }

    USER_PROFILES {
        bigint user_id PK
        varchar username UK
        varchar display_name
        text bio
        varchar avatar_url
        varchar avatar_preview_url
        varchar city
        varchar country
        timestamptz created_at
        timestamptz updated_at
    }

    USER_SESSIONS {
        varchar key PK
        bigint user_id
        varchar refresh_token_hash
        varchar device_info
        timestamptz expires_at
        timestamptz created_at
        timestamptz revoked_at
    }

    COMMUNITIES {
        bigint id PK
        bigint owner_id
        varchar slug UK
        varchar name
        text description
        varchar avatar_url
        varchar avatar_preview_url
        smallint visibility
        smallint status

        bigint members_count_snapshot
        bigint posts_count_snapshot
        timestamptz counters_updated_at

        timestamptz created_at
        timestamptz updated_at
    }

    COMMUNITY_COUNTERS {
        bigint community_id PK
        bigint members_count
        bigint posts_count
        timestamptz updated_at
    }

    COMMUNITY_MEMBERS {
        bigint community_id PK
        int member_bucket PK
        bigint user_id PK
        smallint role
        smallint status
        timestamptz member_since

        varchar user_username_snapshot
        varchar user_display_name_snapshot
        varchar user_avatar_url_snapshot

        varchar community_slug_snapshot
        varchar community_name_snapshot
        varchar community_avatar_url_snapshot
    }

    COMMUNITY_MEMBERS_BY_USER {
        bigint user_id PK
        bigint community_id PK
        smallint role
        smallint status
        timestamptz member_since

        varchar community_slug_snapshot
        varchar community_name_snapshot
        varchar community_avatar_url_snapshot
    }

    POSTS {
        bigint community_id PK
        timestamptz time_bucket PK
        timestamptz published_at PK
        bigint id PK
        bigint author_id

        text body
        smallint status
        smallint visibility

        varchar author_username_snapshot
        varchar author_display_name_snapshot
        varchar author_avatar_url_snapshot

        varchar community_slug_snapshot
        varchar community_name_snapshot
        varchar community_avatar_url_snapshot

        jsonb attachment_refs

        bigint comments_count_snapshot
        bigint reactions_count_snapshot
        timestamptz counters_updated_at

        timestamptz created_at
        timestamptz updated_at
    }

    POSTS_BY_AUTHOR {
        bigint author_id PK
        timestamptz time_bucket PK
        timestamptz published_at PK
        bigint id PK
        bigint community_id

        text body
        smallint status
        smallint visibility

        varchar author_username_snapshot
        varchar author_display_name_snapshot
        varchar author_avatar_url_snapshot

        varchar community_slug_snapshot
        varchar community_name_snapshot
        varchar community_avatar_url_snapshot

        jsonb attachment_refs

        bigint comments_count_snapshot
        bigint reactions_count_snapshot
        timestamptz counters_updated_at
    }

    POST_COUNTERS {
        bigint post_id PK
        int counter_bucket PK
        bigint comments_count
        bigint reactions_count
        timestamptz updated_at
    }

    COMMENTS {
        bigint post_id PK
        bigint id PK

        bigint author_id
        bigint parent_comment_id

        text body
        smallint status

        varchar author_username_snapshot
        varchar author_display_name_snapshot
        varchar author_avatar_url_snapshot

        timestamptz created_at
        timestamptz updated_at
    }

    POST_REACTIONS_BY_POST {
        bigint post_id PK
        int reaction_bucket PK
        bigint user_id PK
        smallint reaction_type
        timestamptz created_at
    }

    USER_REACTIONS_BY_USER {
        bigint user_id PK
        bigint post_id PK
        smallint reaction_type
        timestamptz created_at
    }

    FILE_OBJECTS {
        bigint id PK
        smallint file_type
        varchar mime_type
        varchar storage_bucket
        varchar storage_key
        bigint storage_key_hash
        varchar cdn_url
        varchar preview_url
        bigint size_bytes
        varchar checksum
        smallint status
        timestamptz created_at
        timestamptz updated_at
    }

    FILE_PAYLOADS {
        varchar storage_key PK
        varchar storage_bucket
        bigint size_bytes
        varchar checksum
        timestamptz created_at
    }

    POST_ATTACHMENTS {
        bigint post_id PK
        int sort_order PK
        bigint file_id
        smallint attachment_type
        varchar cdn_url_snapshot
        varchar preview_url_snapshot
        varchar mime_type_snapshot
        bigint size_bytes_snapshot
        timestamptz created_at
    }

    USER_FEED_ITEMS {
        bigint user_id PK
        timestamptz published_at PK
        bigint post_id PK

        bigint author_id
        bigint community_id

        text post_body_snapshot

        varchar author_username_snapshot
        varchar author_display_name_snapshot
        varchar author_avatar_url_snapshot

        varchar community_slug_snapshot
        varchar community_name_snapshot
        varchar community_avatar_url_snapshot

        jsonb attachment_refs_snapshot

        bigint comments_count_snapshot
        bigint reactions_count_snapshot

        double score
        varchar source_type

        timestamptz generated_at
        timestamptz expires_at
    }

    POPULAR_FEED_ITEMS {
        varchar feed_region PK
        timestamptz score_bucket PK
        double score
        bigint post_id PK

        bigint author_id
        bigint community_id

        text post_body_snapshot

        varchar author_username_snapshot
        varchar author_display_name_snapshot
        varchar author_avatar_url_snapshot

        varchar community_slug_snapshot
        varchar community_name_snapshot
        varchar community_avatar_url_snapshot

        jsonb attachment_refs_snapshot

        bigint comments_count_snapshot
        bigint reactions_count_snapshot

        timestamptz generated_at
    }

    CONVERSATIONS {
        bigint id PK
        smallint conversation_type
        varchar home_dc
        timestamptz created_at
        timestamptz updated_at
    }

    CONVERSATION_PARTICIPANTS {
        bigint conversation_id PK
        bigint user_id PK

        smallint status

        varchar user_username_snapshot
        varchar user_display_name_snapshot
        varchar user_avatar_url_snapshot

        timestamptz participant_since
        timestamptz left_at
        timestamptz last_read_at
    }

    CONVERSATION_PARTICIPANTS_BY_USER {
        bigint user_id PK
        bigint conversation_id PK
        smallint status
        smallint conversation_type
        varchar home_dc_snapshot
        timestamptz participant_since
        timestamptz left_at
        timestamptz last_read_at
    }

    USER_CONVERSATIONS {
        bigint user_id PK
        timestamptz last_message_at PK
        bigint conversation_id PK

        varchar home_dc_snapshot
        varchar conversation_title_snapshot
        varchar conversation_avatar_url_snapshot

        bigint last_message_id
        text last_message_body_snapshot
        bigint last_message_sender_id
        varchar last_message_sender_name_snapshot
        varchar last_message_sender_avatar_url_snapshot

        int unread_count

        timestamptz updated_at
    }

    MESSAGES {
        bigint conversation_id PK
        timestamptz time_bucket PK
        bigint id PK

        bigint sender_id
        text body
        smallint status

        varchar sender_username_snapshot
        varchar sender_display_name_snapshot
        varchar sender_avatar_url_snapshot

        timestamptz created_at
        timestamptz edited_at
        timestamptz deleted_at
    }

    SEARCH_INDEX_ENTRIES {
        varchar entity_type PK
        bigint entity_id PK
        text searchable_text
        text preview_text
        varchar avatar_preview_url
        timestamptz updated_at
    }

    EVENT_OUTBOX {
        bigint id PK
        varchar aggregate_type
        bigint aggregate_id
        varchar event_type
        smallint status
        jsonb payload
        timestamptz created_at
        timestamptz processed_at
    }

    USERS ||--|| USER_PROFILES : has
    USERS ||--o{ USER_SESSIONS : owns

    USERS ||--o{ COMMUNITIES : owns
    COMMUNITIES ||--|| COMMUNITY_COUNTERS : has

    USERS ||--o{ COMMUNITY_MEMBERS : user_snapshot_source
    COMMUNITIES ||--o{ COMMUNITY_MEMBERS : community_snapshot_source

    USERS ||--o{ COMMUNITY_MEMBERS_BY_USER : partition_owner
    COMMUNITIES ||--o{ COMMUNITY_MEMBERS_BY_USER : community_snapshot_source

    USERS ||--o{ POSTS : author_snapshot_source
    COMMUNITIES ||--o{ POSTS : community_snapshot_source

    USERS ||--o{ POSTS_BY_AUTHOR : partition_owner
    COMMUNITIES ||--o{ POSTS_BY_AUTHOR : community_snapshot_source

    POSTS ||--|| POST_COUNTERS : counter_source

    POSTS ||--o{ COMMENTS : partition_owner
    USERS ||--o{ COMMENTS : author_snapshot_source

    POSTS ||--o{ POST_REACTIONS_BY_POST : partition_owner
    USERS ||--o{ POST_REACTIONS_BY_POST : reaction_actor

    USERS ||--o{ USER_REACTIONS_BY_USER : partition_owner
    POSTS ||--o{ USER_REACTIONS_BY_USER : reaction_target

    POSTS ||--o{ POST_ATTACHMENTS : partition_owner
    FILE_OBJECTS ||--o{ POST_ATTACHMENTS : file_snapshot_source
    FILE_OBJECTS ||--|| FILE_PAYLOADS : object_ref

    USERS ||--o{ USER_FEED_ITEMS : partition_owner
    POSTS ||--o{ USER_FEED_ITEMS : post_snapshot_source

    POSTS ||--o{ POPULAR_FEED_ITEMS : post_snapshot_source

    CONVERSATIONS ||--o{ CONVERSATION_PARTICIPANTS : partition_owner
    USERS ||--o{ CONVERSATION_PARTICIPANTS : user_snapshot_source

    USERS ||--o{ CONVERSATION_PARTICIPANTS_BY_USER : partition_owner
    CONVERSATIONS ||--o{ CONVERSATION_PARTICIPANTS_BY_USER : dialog_snapshot_source

    USERS ||--o{ USER_CONVERSATIONS : partition_owner
    CONVERSATIONS ||--o{ USER_CONVERSATIONS : dialog_snapshot_source

    CONVERSATIONS ||--o{ MESSAGES : partition_owner
    USERS ||--o{ MESSAGES : sender_snapshot_source

    USERS ||--o{ SEARCH_INDEX_ENTRIES : indexed_as
    COMMUNITIES ||--o{ SEARCH_INDEX_ENTRIES : indexed_as
    POSTS ||--o{ SEARCH_INDEX_ENTRIES : indexed_as
```

### Выбор СУБД для таблиц

| Таблица                             | СУБД / хранилище         | Обоснование                                                                                                           |
| :---------------------------------- | :----------------------- | :-------------------------------------------------------------------------------------------------------------------- |
| `users`                             | PostgreSQL               | Строгая консистентность, уникальность email/phone                                                                     |
| `user_profiles`                     | PostgreSQL               | Профильные данные, уникальность username                                                                              |
| `user_sessions`                     | PostgreSQL + Redis cache | Canonical refresh-сессии в PostgreSQL, быстрый TTL lookup в Redis                                                     |
| `communities`                       | PostgreSQL               | Метаданные сообществ, владельцы, статусы                                                                              |
| `community_counters`                | PostgreSQL               | Каноничные счетчики сообщества                                                                                        |
| `community_members`                 | ScyllaDB                 | Чтение участников и проверка членства по `community_id`; для крупных сообществ нужна защита от одной горячей партиции |
| `community_members_by_user`         | ScyllaDB                 | Read-model для списка сообществ пользователя                                                                          |
| `posts`                             | ScyllaDB                 | Большой объём постов, чтение по `community_id` и `author_id`                                                          |
| `posts_by_author`                   | ScyllaDB                 | Read-model для постов автора                                                                                          |
| `post_counters`                     | ScyllaDB / Redis         | Часто обновляемые счетчики постов                                                                                     |
| `comments`                          | ScyllaDB                 | Высокая нагрузка, чтение комментариев по `post_id`                                                                    |
| `post_reactions_by_post`            | ScyllaDB                 | Уникальность реакции на пост, список лайкнувших                                                                       |
| `user_reactions_by_user`            | ScyllaDB                 | Быстрая проверка `is_liked_by_me`                                                                                     |
| `file_objects`                      | PostgreSQL               | Метаданные файлов и ссылки на объектное хранилище                                                                     |
| `file_payloads`                     | SeaweedFS                | Хранение тяжелого бинарного контента через S3-compatible API                                                          |
| `post_attachments`                  | ScyllaDB                 | Вложения читаются по `post_id`                                                                                        |
| `user_feed_items`                   | ScyllaDB                 | Персональная лента шардируется по `user_id`                                                                           |
| `popular_feed_items`                | ScyllaDB / Redis         | Популярная лента по региону и временному bucket                                                                       |
| `conversations`                     | PostgreSQL               | Метаданные диалогов                                                                                                   |
| `conversation_participants`         | ScyllaDB messenger keyspace | Проверка доступа к перепискам и список участников в `home_dc` диалога                                             |
| `conversation_participants_by_user` | ScyllaDB messenger keyspace | Read-model для проверки/списка диалогов пользователя; хранит `home_dc_snapshot`                                  |
| `user_conversations`                | ScyllaDB messenger keyspace | Read-model inbox пользователя в `home_dc` диалога                                                                  |
| `messages`                          | ScyllaDB messenger keyspace | Высокая нагрузка на историю сообщений, порядок и read-after-write внутри `home_dc`                               |
| `search_index_entries`              | OpenSearch               | Полнотекстовый поиск                                                                                                  |
| `event_outbox`                      | PostgreSQL + Kafka       | Надежная запись событий и асинхронная доставка                                                                        |

### Индексы

Количество строк и QPS для расчёта индексов берутся из [ДЗ 5](./05.md).
Для добавленных денормализованных таблиц используется объём исходной таблицы, которую
они зеркалят: например, `posts_by_author` соответствует `posts`,
`user_reactions_by_user` соответствует `post_reactions`,
`conversation_participants_by_user` соответствует `conversation_participants`.
`popular_feed_items` - ограниченная производная read-model популярной ленты,
поэтому её объём задан отдельно в физической схеме.

| Тип / поле                 | Размер         |
| :------------------------- | :------------- |
| `bigint`                   | 8 Б            |
| `int`                      | 4 Б            |
| `smallint`                 | 2 Б            |
| `timestamptz` / `datetime` | 8 Б            |
| `float` / `double`         | 8 Б            |
| `email`                    | 64 Б           |
| `phone`                    | 16 Б           |
| `username`                 | 32 Б           |
| `slug`                     | 64 Б           |
| `storage_key`              | 128 Б          |
| `storage_key_hash`         | 8 Б            |
| Redis session cache key    | 64 Б           |
| Служебные данные индекса   | 16 Б на запись |

Размер индекса = rows \* (key_size + 16 Б)

| Таблица / структура                 | Ключ / индекс                                                                       | Тип                        | Обоснование                                                                  | Количество строк | Размер ключа | Расчёт                          | Размер |
| :---------------------------------- | :---------------------------------------------------------------------------------- | :------------------------- | :--------------------------------------------------------------------------- | :--------------- | :----------- | :------------------------------ | :----- |
| `users`                             | `id`                                                                                | B-Tree / PK                | Поиск пользователя по ID                                                     | 100 млн          | 8 Б          | 100 млн × (8 + 16)              | 2,4 ГБ |
| `users`                             | `email`                                                                             | Unique B-Tree              | Авторизация и уникальность email                                             | 100 млн          | 64 Б         | 100 млн × (64 + 16)             | 8,0 ГБ |
| `users`                             | `phone`                                                                             | Unique B-Tree              | Авторизация по телефону                                                      | 100 млн          | 16 Б         | 100 млн × (16 + 16)             | 3,2 ГБ |
| `user_profiles`                     | `user_id`                                                                           | B-Tree / PK                | Получение профиля пользователя                                               | 100 млн          | 8 Б          | 100 млн × (8 + 16)              | 2,4 ГБ |
| `user_profiles`                     | `username`                                                                          | Unique B-Tree              | Поиск по username                                                            | 100 млн          | 32 Б         | 100 млн × (32 + 16)             | 4,8 ГБ |
| `communities`                       | `id`                                                                                | B-Tree / PK                | Получение сообщества                                                         | 5 млн            | 8 Б          | 5 млн × (8 + 16)                | 120 МБ |
| `communities`                       | `slug`                                                                              | Unique B-Tree              | Поиск сообщества по URL                                                      | 5 млн            | 64 Б         | 5 млн × (64 + 16)               | 400 МБ |
| `community_counters`                | `community_id`                                                                      | B-Tree / PK                | Получение счетчиков сообщества                                               | 5 млн            | 8 Б          | 5 млн × (8 + 16)                | 120 МБ |
| `community_members`                 | `partition key: (community_id, member_bucket), clustering key: user_id`             | Primary Key ScyllaDB       | Список участников сообщества без огромной партиции                           | 1 млрд           | 20 Б         | включён в объём таблицы         | —      |
| `community_members_by_user`         | `partition key: user_id, clustering key: community_id`                              | Отдельная таблица ScyllaDB | Список сообществ пользователя                                                | 1 млрд           | 16 Б         | включён в объём таблицы         | —      |
| `posts`                             | `partition key: (community_id, time_bucket), clustering key: published_at DESC, id` | Primary Key ScyllaDB       | Чтение ленты сообщества без бесконечной партиции                             | 10 млрд          | 32 Б         | включён в объём таблицы         | —      |
| `posts_by_author`                   | `partition key: (author_id, time_bucket), clustering key: published_at DESC, id`    | Отдельная таблица ScyllaDB | Чтение постов автора без бесконечной партиции                                | 10 млрд          | 32 Б         | включён в объём таблицы         | —      |
| `post_counters`                     | `partition key: post_id, clustering key: counter_bucket`                            | Primary Key ScyllaDB       | Счетчики поста, для горячих постов - несколько bucket                        | 10 млрд          | 12 Б         | включён в объём таблицы         | —      |
| `comments`                          | `partition key: post_id, clustering key: created_at, id`                            | Primary Key ScyllaDB       | Чтение комментариев поста по порядку                                         | 30 млрд          | 24 Б         | включён в объём таблицы         | —      |
| `post_reactions_by_post`            | `partition key: (post_id, reaction_bucket), clustering key: user_id`                | Primary Key ScyllaDB       | Уникальность реакции без hot partition у вирусных постов                     | 100 млрд         | 20 Б         | включён в объём таблицы         | —      |
| `user_reactions_by_user`            | `partition key: user_id, clustering key: post_id`                                   | Primary Key ScyllaDB       | Проверка лайка пользователя                                                  | 100 млрд         | 16 Б         | включён в объём таблицы         | —      |
| `file_objects`                      | `id`                                                                                | B-Tree / PK                | Получение метаданных файла                                                   | 21 млрд          | 8 Б          | 21 млрд × (8 + 16)              | 504 ГБ |
| `file_objects`                      | `storage_key_hash`                                                                  | B-Tree                     | Служебная проверка связи с объектным хранилищем; горячий lookup идет по `id` | 21 млрд          | 8 Б          | 21 млрд × (8 + 16)              | 504 ГБ |
| `post_attachments`                  | `partition key: post_id, clustering key: sort_order`                                | Primary Key ScyllaDB       | Получение вложений поста в нужном порядке                                    | 21 млрд          | 12 Б         | включён в объём таблицы         | —      |
| `user_feed_items`                   | `partition key: user_id, clustering key: published_at DESC, post_id`                | Primary Key ScyllaDB       | Чтение персональной ленты                                                    | 300 млрд         | 24 Б         | включён в объём таблицы         | —      |
| `popular_feed_items`                | `partition key: (feed_region, score_bucket), clustering key: score DESC, post_id`   | Primary Key ScyllaDB       | Топ постов по региону и временному bucket                                    | 1 млрд           | 88 Б         | включён в объём таблицы         | —      |
| `user_sessions`                     | `key`                                                                               | B-Tree / PK                | Canonical refresh-сессия                                                     | 200 млн          | 64 Б         | 200 млн × (64 + 16)             | 16 ГБ  |
| Redis session cache                 | `session:{key}`                                                                     | Redis Key                  | Быстрая проверка access/session key                                          | active TTL set   | 64 Б         | зависит от TTL                  | —      |
| `conversations`                     | `id`                                                                                | B-Tree / PK                | Получение диалога                                                            | 1 млрд           | 8 Б          | 1 млрд × (8 + 16)               | 24 ГБ  |
| `conversation_participants`         | `partition key: conversation_id, clustering key: user_id`                           | Primary Key ScyllaDB       | Проверка доступа к диалогу                                                   | 5 млрд           | 16 Б         | включён в объём таблицы         | —      |
| `conversation_participants_by_user` | `partition key: user_id, clustering key: conversation_id`                           | Отдельная таблица ScyllaDB | Список диалогов пользователя                                                 | 5 млрд           | 16 Б         | включён в объём таблицы         | —      |
| `user_conversations`                | `partition key: user_id, clustering key: last_message_at DESC, conversation_id`     | Primary Key ScyllaDB       | Inbox пользователя                                                           | 1 млрд           | 24 Б         | включён в объём таблицы         | —      |
| `messages`                          | `partition key: (conversation_id, time_bucket), clustering key: id DESC`            | Primary Key ScyllaDB       | История диалога без бесконечной партиции                                     | 100 млрд         | 24 Б         | включён в объём таблицы         | —      |
| `search_index_entries`              | `entity_type, entity_id, searchable_text`                                           | Inverted Index OpenSearch  | Документ сущности и полнотекстовый поиск                                     | 10,1 млрд        | text         | 20–50% от индексируемого текста | 2–6 ТБ |
| `event_outbox`                      | `(status, created_at)`                                                              | B-Tree                     | Выборка необработанных событий                                               | 100 млн active   | 10 Б         | 100 млн × (10 + 16)             | 2,6 ГБ |

Пояснения по ключам:

- PostgreSQL B-Tree индексы оставлены только там, где есть точечный lookup или уникальность: `id`, `email`, `phone`, `username`, `slug`.
- Для `file_objects` горячий lookup идет по короткому `id`. Индекс по длинному `storage_key` не используется как основной путь чтения, потому что при 21 млрд файлов он даёт около `3 ТБ` только на ключи; вместо него оставлен короткий `storage_key_hash` для служебной проверки и поиска редких расхождений с объектным хранилищем.
- В ScyllaDB ключи выбраны по основным запросам: лента пользователя по `user_id`, сообщения по `conversation_id`, комментарии по `post_id`, сообщества пользователя по `user_id`.
- Для `posts`, `posts_by_author` и `messages` добавлен `time_bucket`, потому что история крупного сообщества, автора или диалога не должна бесконечно расти в одной партиции.
- Для мессенджера `conversations.home_dc` определяет домашний ДЦ диалога и
  соответствующий keyspace: `messenger_msk`, `messenger_spb` или
  `messenger_ekb`. `messages`, `conversation_participants`,
  `conversation_participants_by_user` и `user_conversations` пишутся с
  `LOCAL_QUORUM` внутри `home_dc`; асинхронные копии в другие ДЦ используются
  для failover.
- Для `community_members` и `post_reactions_by_post` добавлены hash-bucket, чтобы крупное сообщество или вирусный пост не создавали одну огромную и горячую партицию.
- Для точечной проверки членства или реакции bucket вычисляется из `user_id`. Для чтения списка участников или списка лайкнувших сервис читает фиксированное число bucket параллельно и объединяет результат.
- Для `popular_feed_items` `score_bucket` входит в partition key, чтобы не складывать всю региональную популярную ленту в одну горячую партицию.
- Партиционирование не уменьшает общий размер индекса на одном узле. Оно только разбивает таблицу на управляемые части и позволяет работать с меньшими локальными индексами. Если цель - уменьшить объём данных или индекс в RAM одного узла, нужно шардирование или изменение ключа доступа.

### Шардирование

В этом разделе разделяем два понятия:

- **Шардирование** - распределение данных между разными узлами/шардами.
- Один узел не держит write QPS
- Один узел не держит объём данных или рабочий набор индексов в RAM
- **Партиционирование** - деление таблицы или логической модели на управляемые части внутри выбранного хранилища.
- ограничение размера одной партиции
- временных bucket
- удаления старых данных
- снижения hot partition
- НЕ уменьшает общий объём индексов на одном узле

Отметим, что в ScyllaDB шардирование работает из коробки: приложение выбирает partition key,
а ScyllaDB распределяет его по узлам кластера.

| Таблица                             | Хранилище        | Ключ распределения               | Обоснование                                                                                                                                                                                                                                                                                                                                 |
| :---------------------------------- | :--------------- | :------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `community_members`                 | ScyllaDB         | `(community_id, member_bucket)`  | Вся таблица ≈ `56 ГБ` и `13,61` write QPS могла бы жить на одном узле, но крупное сообщество может создать hot key для списка участников, проверки членства и изменений состава. Репликация помогает чтению, но не распределяет запись и консистентные изменения членства; `member_bucket` распределяет нагрузку внутри крупного сообщества |
| `community_members_by_user`         | ScyllaDB         | `user_id`                        | Обратная read-model на `1 млрд` строк распределяется по владельцу списка сообществ                                                                                                                                                                                                                                                          |
| `posts`                             | ScyllaDB         | `(community_id, time_bucket)`    | Объём `10 млрд` строк не держим на одном узле; основной запрос - лента сообщества; `time_bucket` ограничивает партицию крупного сообщества                                                                                                                                                                                                  |
| `posts_by_author`                   | ScyllaDB         | `(author_id, time_bucket)`       | Read-model постов автора дублирует порядок объёма `posts`; `time_bucket` ограничивает партицию активного автора                                                                                                                                                                                                                             |
| `post_counters`                     | ScyllaDB / Redis | `post_id`                        | Счетчики читаются и обновляются по посту; `counter_bucket` применяется для горячих постов, где один ключ не держит частые инкременты                                                                                                                                                                                                        |
| `comments`                          | ScyllaDB         | `post_id`                        | Объём `30 млрд` строк не держим на одном узле; основной запрос - комментарии под конкретным постом                                                                                                                                                                                                                                          |
| `post_reactions_by_post`            | ScyllaDB         | `(post_id, reaction_bucket)`     | Объём `100 млрд` строк и `17 013,89` write QPS требуют распределения; `reaction_bucket` снижает hot partition у вирусных постов                                                                                                                                                                                                             |
| `user_reactions_by_user`            | ScyllaDB         | `user_id`                        | Обратная read-model на `100 млрд` строк распределяется по пользователям для быстрых проверок реакций в ленте                                                                                                                                                                                                                                |
| `post_attachments`                  | ScyllaDB         | `post_id`                        | Вложения всегда читаются вместе с постом, поэтому распределение идет по `post_id`                                                                                                                                                                                                                                                           |
| `user_feed_items`                   | ScyllaDB         | `user_id`                        | Объём `300 млрд` строк и `100 000` write QPS fanout распределяются по владельцам персональных лент                                                                                                                                                                                                                                          |
| `popular_feed_items`                | ScyllaDB / Redis | `(feed_region, score_bucket)`    | Региональная популярная лента может быть горячей; `score_bucket` не даёт всей региональной ленте попасть в одну партицию                                                                                                                                                                                                                    |
| `conversation_participants`         | ScyllaDB         | `conversation_id`                | Объём `5 млрд` строк распределяется по диалогам; проверка доступа и список участников выполняются по `conversation_id`                                                                                                                                                                                                                      |
| `conversation_participants_by_user` | ScyllaDB         | `user_id`                        | Обратная read-model на `5 млрд` строк распределяется по пользователям для списка их диалогов                                                                                                                                                                                                                                                |
| `user_conversations`                | ScyllaDB         | `user_id`                        | Inbox читается по пользователю, поэтому ключ распределения совпадает с владельцем inbox                                                                                                                                                                                                                                                     |
| `messages`                          | ScyllaDB         | `(conversation_id, time_bucket)` | Объём `100 млрд` строк и `255 208,33` write QPS требуют распределения; история читается по диалогу, а `time_bucket` ограничивает размер партиции                                                                                                                                                                                            |
| `search_index_entries`              | OpenSearch       | document id / routing key        | Документы распределяются по shard OpenSearch, чтобы индекс не жил на одном узле                                                                                                                                                                                                                                                             |
| `file_payloads`                     | SeaweedFS        | `storage_key`                    | Объём файлового архива измеряется ПБ, поэтому бинарные объекты распределяются средствами объектного хранилища                                                                                                                                                                                                                               |
| Redis session cache                 | Redis Cluster    | `session:{key}`                  | Кеш активных session/access ключей распределяется по hash slot из-за частых auth lookup                                                                                                                                                                                                                                                     |

PostgreSQL-таблицы `users`, `user_profiles`, `communities`, `community_counters`, `file_objects`, `conversations` и `event_outbox` здесь не шардируются.
Их write QPS и размер индексов укладываются в выбранный PostgreSQL-кластер.

#### Партиционирование

Ключи ScyllaDB с `time_bucket` / `bucket` описаны выше в разделе шардирования,
чтобы не дублировать одну и ту же схему в двух таблицах.

| Таблица / структура | Хранилище  | Ключ партиционирования | Обоснование                                                                                          |
| :------------------ | :--------- | :--------------------- | :--------------------------------------------------------------------------------------------------- |
| `event_outbox`      | PostgreSQL | `created_at`           | Партиции по времени упрощают батчевую обработку и удаление старых событий                            |
| `user_sessions`     | PostgreSQL | `expires_at`           | Позволяет удалять истекшие refresh-сессии целыми партициями; шардирование по QPS/объёму не требуется |

### Резервирование

| СУБД       | Схема                                                                         | Обоснование                                                                                                       |
| :--------- | :---------------------------------------------------------------------------- | :---------------------------------------------------------------------------------------------------------------- |
| PostgreSQL | 3 узла Patroni в primary DC `MSK` + по 1 async standby в `SPB` и `EKB`        | Совпадает с расчетом серверов в ДЗ 11; запись идет в `MSK`, чтение с replica только там, где допустимо отставание |
| ScyllaDB social keyspace | Единый multi-DC keyspace, `NetworkTopologyStrategy` с RF=1 на каждый ДЦ, суммарно RF=3, consistency level `QUORUM` | Посты, комментарии, реакции, ленты и подписки имеют три копии по ДЦ; строгие операции подтверждаются двумя репликами из трех |
| ScyllaDB messenger keyspace family | `messenger_msk`, `messenger_spb`, `messenger_ekb`: данные диалога пишутся в keyspace своего `home_dc` с RF=3 и `LOCAL_QUORUM`; меж-ДЦ копии обновляются асинхронно | Быстрая отправка сообщений, порядок и read-after-write внутри домашнего ДЦ диалога |
| SeaweedFS  | Репликация объектов между основными ДЦ                                        | Защита файлов от потери                                                                                           |

### Резервное копирование

| Хранилище      | Схема backup / restore                                                                                                                                                                             |
| :------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PostgreSQL     | Ежедневный full backup, WAL-архивация и Point-in-Time Recovery. Backup хранится минимум в двух ДЦ. Перед failover в другой ДЦ проверяется lag standby-реплики.                                     |
| ScyllaDB       | Регулярные snapshots по keyspace, инкрементальная выгрузка SSTable в объектное хранилище, repair по расписанию. Для лент и кешируемых read-model допускается пересборка из canonical data и Kafka. |
| SeaweedFS      | RF=3 между основными ДЦ, периодическая сверка checksum, lifecycle для orphan-объектов. Metadata файлов восстанавливается из PostgreSQL.                                                            |
| OpenSearch     | Snapshot индексов в объектное хранилище. При полной потере индекс можно пересобрать из PostgreSQL, ScyllaDB и Kafka-событий.                                                                       |
| Redis          | Backup не требуется для кешей. Для Redis session cache source of truth находится в PostgreSQL.                                                                                                     |
| Kafka          | RF=3 внутри ДЦ, retention по доменам. Для критичных топиков включена асинхронная меж-ДЦ репликация, чтобы после failover восстановить read-model.                                                  |
